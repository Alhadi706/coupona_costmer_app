# خطة تنفيذ كوبونا الشاملة (Kupuna Master Execution Plan)

> **المصدر:** هذه الخطة مبنية على "وثيقة كوبونا الشاملة والنهائية" الكاملة (18 قسمًا، بعد إلحاق القسم 18 "نظام التصميم البصري الكامل" بتاريخ 2026-08-07) التي زوّدنا بها المستخدم.
> **الغرض من هذا الملف:** أي Agent يبدأ من هنا يجب أن يقدر ينفذ المشروع فازة (Phase) فازة، **بالترتيب الحرفي**، دون قفز، ودون كسر أي شيء يعمل حاليًا.
> **حالة المستودع وقت كتابة هذه الخطة:** تطبيق Flutter (`lib/`) + خادم Node/Express + PostgreSQL (`backend/server.js`, ~1470 سطر) يعمل حاليًا على السيرفر البعيد `154.12.117.175` (راجع `KUPUNA_SERVER_MIGRATION_REPORT.md`). يوجد نظام مستخدمين مسطّح (`users`) بدون أدوار متعددة، نظام نقاط/كاش باك مسطّح واحد (`lib/modules/accounting/`)، ودورة حياة كوبون أساسية (`lib/modules/coupon_lifecycle/`). **الوثيقة الشاملة تتطلب إعادة هيكلة جوهرية** (User موحّد + أدوار متعددة + محركات منفصلة) — هذه ليست ميزة إضافية بسيطة، هي تحوّل معماري.

---

## 0. قواعد إلزامية تحكم تنفيذ كل فازة (اقرأها قبل أي سطر كود)

1. **الترتيب صارم.** لا تُنفَّذ فازة قبل اجتياز فازة سابقة لأمر التحقق الخاص بها بنجاح 100%.
2. **لا كسر لما يعمل.** التطبيق حاليًا Live ومُستخدَم. كل فازة يجب أن تنتهي والتطبيق **قابل للتشغيل والترجمة (compile) بدون أي خطأ**. لا تُترك الفازة "نص مكتملة" بين جلسة وأخرى.
3. **أمر تحقق إلزامي بعد كل فازة** (محدد أدناه لكل فازة). لا تنتقل للفازة التالية إذا فشل أمر التحقق.
4. **الحد الأدنى من الأوامر بعد أي فازة كود Flutter:**
   ```powershell
   flutter analyze
   flutter test
   ```
   يجب أن تكون النتيجة "No issues found!" و كل الاختبارات "All tests passed!" قبل الانتقال.
5. **التوافق العكسي أثناء الانتقال.** أي تغيير جذري في نموذج البيانات (مثل تقسيم النقاط لطبقات) يُبنى **بجانب** النظام القديم أولًا (Feature Flag / عمود جديد)، ثم يتم القطع (Cutover) فقط بعد التأكد، ثم تُحذف الشيفرة القديمة كخطوة منفصلة موثّقة.
6. **نمط عمل موثّق سلفًا لهذا المستودع (راجع `/memories/repo/kupuna-ops.md`):** تنفيذ Module واحد في كل مرة + تقرير Markdown مختصر بعد كل فازة (اسم الملف: `PHASE_<n>_<slug>_REPORT.md`) + اختبارات فعلية، وليس وعود.
7. **مزامنة السيرفر البعيد:** أي تعديل Backend يُرفع للسيرفر `154.12.117.175` بنفس نمط `tar → scp → extract` الموثّق مسبقًا، ويُتحقق منه بـ `ls` و`grep` بعد الرفع مباشرة، قبل اعتبار الفازة منتهية.
8. **الالتزام بقواعد الترجمة (i18n):** أي نص جديد يظهر للمستخدم يُضاف كمفتاح `.tr()` في **كل** ملفات `assets/lang/*.json` (16 لغة)، ممنوع نص عربي/إنجليزي مباشر داخل الودجات الجديدة.
9. **نمط الشاشات المُضمَّنة (Embedded):** أي شاشة جديدة قد تُستخدَم داخل تبويبات الرئيسية يجب أن يكون لها وضع `embedded` (بدون Scaffold/AppBar مستقل) لتفادي ازدواج شريط التنقل — نفس النمط المتبع حاليًا في `OffersScreen.embedded()` وغيرها.
10. **لا Vendor Lock-in ولا حذف بيانات مستخدمين حقيقيين.** أي Migration على قاعدة بيانات الإنتاج يُسبَق بنسخة احتياطية (`pg_dump`) ويُختبر أولًا على نسخة محلية/تجريبية.
11. عند أي غموض في تفسير الوثيقة أثناء التنفيذ، الأولوية دائمًا لـ"القسم 17: ملخص القرارات النهائية السريع" لأنه يمثل القرار المعتمد النهائي.

---

## خريطة الفازات (Overview)

| # | الفازة | يقابلها في الوثيقة |
|---|--------|---------------------|
| 0 | تجهيزات وشبكة أمان قبل البدء | - |
| 1 | نموذج User الموحّد + جداول ملفات الأدوار (DB) | القسم 1، 10، Sprint 0 |
| 2 | واجهة "أدواري" وتبديل الأدوار (UI) | القسم 2.1، 6.1، 14.3 |
| 3 | تفعيل الأدوار المدفوعة + آلة حالة الاشتراك الأساسية | القسم 2.2–2.5، 10 |
| 4 | إدارة الفروع وفريق العمل (تاجر) + فريق العلامة التجارية | القسم 3(بند5)، 6.2، 6.3، 7.1، 7.3، 10 |
| 5 | محرك النقاط v2 — طبقة التاجر (كسور + صلاحية 12 شهرًا + استرجاع) | القسم 4.2، 3(بند3،4،6)، Sprint 1 |
| 6 | تدفق QR/POS + دور الكاشير الأساسي (منح نقاط + استبدال بين الفروع) | القسم 4.12، 6.4، 7.2، 3(بند5)، Sprint 1 |
| 7 | محرك مكافحة الاحتيال + سياسة الاحتفاظ بالبيانات | القسم 4.4، 3(بند8،9)، Sprint 2 |
| 8 | التعرف السطري على الفاتورة + دليل المنتجات + مطابقة العلامات | القسم 4.1، 4.2(علامة)، 6.3، Sprint 3 |
| 9 | المجتمعات + العروض + الإشعارات | القسم 4.3، 4.5، 4.9، Sprint 4 |
| 10 | محرك البلاغات والاعتراضات | القسم 4.10، 5.5، Sprint 5 |
| 11 | الضمان/التسوية + تبادل النقاط + سوق المكافآت + استلام الكاشير | القسم 4.6، 4.8، 4.12، Sprint 6 |
| 12 | إعلانات الأفراد والتوريد | القسم 4.11، Sprint 7 |
| 13 | محرك إدارة الأدوار والاشتراكات الكامل | القسم 4.13، 2.3–2.5، Sprint 8 |
| 14 | التنبؤ بالسلوك + Loyalty Health Score | القسم 4.7، Sprint 9 |
| 15 | مصفوفة الصلاحيات + الأمان الشامل (تدقيق وإنفاذ) | القسم 7، 8 |
| 16 | كتالوج الأخطاء والحواف + لوحة الأدمن الموحّدة + تدقيق التصميم/i18n | القسم 9، 6.5، 14 |
| 17 | تدقيق شامل نهائي (End-to-End) + تسليم | القسم 15، 17 |
| 18 | سد فجوة Flutter متعددة الأدوار: لوحة التاجر + لوحة العلامة + وضع الكاشير + شاشتا التبادل وQR | القسم 6.2، 6.3، 6.4، 7.2 |
| 19 | البنية التحتية لنظام التصميم (Design Tokens + الخطوط + الثيمات حسب الدور) | القسم 18-أ |
| 20 | مكتبة المكوّنات المشتركة (Bottom Navbar، Top Tabs، Offer Card، Chat Bubble، Dual Wallet Rings، Loyalty Ring، وضع الكاشير، Status Pill) | القسم 18-ب |
| 21 | تطبيق نظام التصميم على شاشات تطبيق العميل | القسم 18-ج |
| 22 | تطبيق نظام التصميم على لوحة تحكم التاجر | القسم 18-د |
| 23 | تطبيق نظام التصميم على لوحة تحكم العلامة التجارية | القسم 18-هـ |
| 24 | تطبيق نظام التصميم على لوحة إدارة كوبونا (Admin) | القسم 18-و |
| 25 | تدقيق شامل نهائي لنظام التصميم (لا خلط ألوان + القاعدة العامة للشاشات المتبقية) | القسم 18-ز |

> **إجمالي الفازات الآن: 26 (من 0 إلى 25)** بعد اعتماد الفازة 18 كفازة Flutter مخصصة لسد فجوة الأدوار غير العميل، وبعد إلحاق القسم 18 (نظام التصميم البصري الكامل) بالوثيقة الأصلية وتوزيعه على الفازات 19–25 المخصصة لتطبيقه شاشة بشاشة.

---

## الفازة 0 — تجهيزات وشبكة أمان قبل البدء

**الهدف:** التأكد أن نقطة الانطلاق سليمة وقابلة للقياس، قبل أي تعديل.

**المهام:**
- إنشاء فرع Git مخصص: `feature/kupuna-master-spec`.
- نسخة احتياطية من قاعدة البيانات الحالية (`pg_dump`) وحفظها في `/opt/projects/kupuna/backups` على السيرفر.
- تشغيل `flutter analyze` و`flutter test` على الحالة الحالية وتوثيق أنها نظيفة (خط أساس Baseline).
- التأكد أن `backend/server.js` يعمل ويستجيب على `/api/health`.
- النص الكامل والحرفي لـ"وثيقة كوبونا الشاملة والنهائية" محفوظ بالفعل في `docs/KUPUNA_MASTER_SPEC.md` كمرجع دائم لا يتغير. **هذا الملف الحالي (`KUPUNA_MASTER_EXECUTION_PLAN.md`) هو ملخص تنفيذي كافٍ للتنفيذ بحد ذاته**، لكن عند أي التباس في تفصيل رقمي دقيق (نسبة، مهلة زمنية، معادلة) يُرجع دائمًا للنص الأصلي في `docs/KUPUNA_MASTER_SPEC.md`.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ على السيرفر: `curl -s http://154.12.117.175:3002/api/health` يرجع `{"status":"ok"}` أو ما يعادلها.

**معيار القبول:** "الفازة 0 جاهزة عندما يكون `flutter analyze` و`flutter test` نظيفين تمامًا، والفرع الجديد موجود، والنسخة الاحتياطية للقاعدة موجودة فعليًا على القرص."

---

## الفازة 1 — نموذج User الموحّد + جداول ملفات الأدوار (DB)

**المرجع:** القسم 1.2، 1.3، 10، Sprint 0.

**المهام (Backend):**
- **لا تُحذف/تُعدَّل** جدول `users` الحالي بشكل هدّام؛ يبقى كما هو (id, phone, password_hash...).
- إضافة جداول جديدة (كلها بمفتاح خارجي `user_id → users.id`):
  - `customer_profiles` (تُنشأ تلقائيًا لكل مستخدم جديد عند التسجيل — Trigger أو كود في `/api/auth/signup`).
  - `merchant_profiles` (يشمل `point_value`، `business_name`، `commercial_registration`، `status`).
  - `brand_profiles` (نفس فكرة merchant_profiles، مستقلة).
  - `cashier_profiles` (`user_id`, `merchant_id`, صلاحيات ثابتة).
- Migration Script واحد بالاسم `backend/migrations/001_role_profiles.sql`، يُشغَّل مرة واحدة، **idempotent** (`CREATE TABLE IF NOT EXISTS`).
- Endpoint جديد: `GET /api/roles/me` يرجع `{customer: true, merchant: false, brand: false, cashier: []}` بناءً على وجود سجلات في الجداول أعلاه.
- تعديل `/api/auth/signup` لإنشاء سجل `customer_profiles` تلقائيًا لكل مستخدم جديد (لا يُطلب أبدًا يدويًا).

**المهام (Flutter):**
- `lib/services/company_server_service.dart`: إضافة `getMyRoles()` يستدعي `/api/roles/me`.
- لا شاشات جديدة بعد — هذه فازة بيانات فقط.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ اختبار SQL مباشر على السيرفر (أو محليًا على نسخة تجريبية):
```sql
SELECT to_regclass('public.customer_profiles'),
       to_regclass('public.merchant_profiles'),
       to_regclass('public.brand_profiles'),
       to_regclass('public.cashier_profiles');
```
كلها يجب أن ترجع اسم الجدول (غير NULL).
+ استدعاء تجريبي: `curl -H "Authorization: Bearer <token>" http://.../api/roles/me` يرجع JSON صحيح بدون خطأ 500.

**معيار القبول:** "الفازة 1 جاهزة عندما يُنشأ `customer_profiles` تلقائيًا لكل مستخدم جديد، وتُرجع `/api/roles/me` بيانات صحيحة، دون أي كسر في تسجيل الدخول أو التسجيل الحالي."

---

## الفازة 2 — واجهة "أدواري" وتبديل الأدوار (UI)

**المرجع:** القسم 2.1، 6.1 (صف "أدواري")، 14.3.

**المهام (Flutter):**
- شاشة جديدة `lib/screens/my_roles_screen.dart`: تعرض الأدوار المفعّلة (من `getMyRoles()`) + حالة الاشتراك (لاحقًا في الفازة 3) + زر تفعيل دور جديد (زر معطّل/يوجّه لصفحة "قريبًا" حتى الفازة 3).
- أيقونة/زر تبديل دور ثابتة في الشريط العلوي (`lib/widgets/` أو داخل `home_screen.dart` AppBar) — تفتح قائمة اختيار الدور.
- **قاعدة تصميم صارمة من الوثيقة:** عند اختيار دور، تتحول الواجهة بالكامل (لا تبويبات مدمجة). التنفيذ العملي المرحلي: التبديل الفعلي للوحات التاجر/العلامة الكاملة يُبنى تدريجيًا في الفازات اللاحقة؛ في هذه الفازة نبني فقط **آلية التبديل والتصميم** (Theme per role كما في القسم 14.1) بينما تبقى لوحات تاجر/علامة "قيد الإنشاء" placeholder.
- إضافة مفاتيح ترجمة جديدة إلى **كل** ملفات `assets/lang/*.json` (my_roles_title، activate_merchant_role، activate_brand_role، role_customer، role_merchant، role_brand، role_cashier...).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ اختبار ودجت جديد: `test/my_roles_screen_test.dart` يتحقق من:
  - ظهور دور "عميل" دائمًا كمُفعّل.
  - عدم ظهور تبويبات عميل/تاجر جنبًا لجنب في نفس الشجرة (Widget tree) في آنٍ واحد.

**معيار القبول:** "الفازة 2 جاهزة عندما تعرض شاشة أدواري دور العميل مفعّلًا دائمًا، ويعمل زر التبديل، ولا تظهر أي مفاتيح ترجمة مفقودة عند التبديل بين العربية والإنجليزية."

---

## الفازة 3 — تفعيل الأدوار المدفوعة + آلة حالة الاشتراك الأساسية

**المرجع:** القسم 2.2، 2.3، 2.4، 2.5، القسم 10 (`Subscription`).

**المهام (Backend):**
- جدول `subscriptions` (`role_profile_id`, `role_type` ['merchant'|'brand'], `plan_type`, `status` ['trial'|'active'|'grace_period'|'suspended'], `trial_duration_days` [30-90]، `trial_start_date`، `trial_end_date`، `billing_cycle`، `next_billing_date`، `payment_method_ref`).
- Endpoints:
  - `POST /api/roles/merchant/request` (بيانات النشاط + الباقة) → ينشئ `merchant_profiles` بحالة `pending_admin_review` + طلب عضوية للأدمن. **لا نشاط قبل الموافقة.**
  - `POST /api/roles/brand/request` (بنفس المنطق، مستقل تمامًا).
  - `POST /api/admin/role-requests/:id/approve` و`/reject` (تفعيل الفازة الإدارية الأساسية، حتى لو لوحة الأدمن الكاملة لاحقًا).
  - عند الموافقة: تُنشأ `subscriptions` بحالة `trial` ومدة افتراضية (تُقرأ من إعداد أدمن قابل للتغيير، افتراضي مثلاً 30).
- Job/Endpoint للتحقق الدوري من انتهاء `trial_end_date` وتحويل الحالة تلقائيًا حسب آلة الحالة في القسم 2.5 (trial → grace_period → suspended)، مع تنبيهات عند 7 أيام و1 يوم (تُسجَّل في جدول إشعارات، حتى لو الإرسال الفعلي عبر FCM يُستكمل بالفازة 8).
- **قاعدة إلزامية:** تعطيل/تعليق الاشتراك **لا يمس** `customer_profiles` لنفس المستخدم إطلاقًا — يُختبر صراحة.

**المهام (Flutter):**
- شاشة طلب تفعيل دور (نموذج بيانات + اختيار باقة) لكل من التاجر والعلامة.
- شاشة "حالة الطلب" (قيد المراجعة / مرفوض مع السبب / مقبول).
- تحديث `my_roles_screen.dart` لعرض حالة الاشتراك الفعلية (trial/active/grace_period/suspended) بدل الـ placeholder.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ اختبار منطقي جديد `test/subscription_state_machine_test.dart` يغطي **كل** الانتقالات في القسم 2.5 (بما فيها الانتقالات الممنوعة يجب أن تُرفض).
+ سيناريو يدوي/سكربت: إنشاء طلب تاجر → موافقة أدمن → تحقق أن `subscriptions.status = 'trial'` → محاكاة انتهاء `trial_end_date` → تحقق التحول لـ `grace_period` ثم `suspended` دون التأثير على بيانات `customer_profiles`.

**معيار القبول:** "الفازة 3 جاهزة عندما تمر آلة حالة الاشتراك بكل الانتقالات المعتمدة في القسم 2.5 فقط، ولا تتأثر بيانات دور العميل لنفس المستخدم بأي حالة من حالات التعليق أو الإيقاف."

---

## الفازة 4 — إدارة الفروع وفريق العمل (تاجر) + فريق العلامة التجارية

**المرجع:** القسم 3 (بند 5)، القسم 6.2 (إدارة الفروع، إدارة الموظفين)، القسم 6.3 (فريق العلامة)، القسم 7.1، 7.3، القسم 10 (`Branch`, `BranchManagerPermissions`).

**المهام (Backend):**
- جدول `branches` (merchant_id, name, address, location, status) — تاجر واحد يقدر يملك أكثر من فرع.
- جدول `branch_manager_permissions` (branch_id/user_id + أعمدة boolean لكل صلاحية من جدول القسم 7.1: مراجعة الفواتير، إنشاء عروض، إدارة القروب، عرض التقارير، عرض التسويات/الضمان، إضافة موظفي كاشير، الرد على البلاغات، تعديل `point_value`).
- **قاعدة إلزامية:** عند إنشاء أي مدير فرع جديد، كل الصلاحيات تُنشأ بقيمة `false` افتراضيًا دون استثناء.
- Endpoints: `POST /api/merchant/branches`, `GET /api/merchant/branches`, `POST /api/merchant/branches/:id/managers`, `PATCH /api/merchant/branches/:id/managers/:userId/permissions`.
- جدول/آلية مماثلة لفريق العلامة التجارية (`brand_team_members` + صلاحياتهم الخاصة: إدارة دليل المنتجات، الوصول لخريطة التوزيع الجغرافي) — مستقلة تمامًا عن جداول التاجر.
- ربط `cashier_profiles` (من الفازة 1) بـ`branch_id` تحديدًا وليس فقط `merchant_id`، لأن الكاشير يعمل بفرع واحد محدد.

**المهام (Flutter):**
- شاشة "إدارة الفروع" (قائمة فروع + زر إضافة فرع) في لوحة التاجر.
- شاشة "إدارة الموظفين" (قائمة موظفين/مديري فروع + قائمة اختيار Checkbox للصلاحيات).
- شاشة مكافئة لفريق العلامة التجارية.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ `test/branch_manager_permissions_default_test.dart`: إنشاء مدير فرع جديد عبر API/منطق Dart المقابل → التحقق أن **كل** أعمدة الصلاحيات `false`.
+ اختبار: إضافة فرعين لنفس التاجر + كاشير مرتبط بفرع محدد → التحقق أن الكاشير لا يظهر إلا ضمن فرعه.

**معيار القبول:** "إدارة الفروع والموظفين جاهزة عندما يُنشأ أي مدير فرع جديد بكل الصلاحيات `false`، ويرتبط كل كاشير بفرع واحد محدد بدقة."

---

## الفازة 5 — محرك النقاط v2: طبقة التاجر (كسور + صلاحية 12 شهرًا + استرجاع)

**المرجع:** القسم 4.2 (جزء التاجر)، القسم 3 (بند 3، 4، 6)، Sprint 1.

**المهام (Backend):**
- إضافة `point_value` فعليًا على `merchant_profiles` (أُضيف هيكليًا بالفازة 1، الآن يُستخدم حسابيًا).
- جدول `customer_merchant_fraction_balance` (customer_id, merchant_id, fraction_balance).
- جدول/طبقة `points_ledger_merchant` (منفصل عن `ledger_entries` القديم — القديم يبقى شغّالًا كـ Feature Flag موازٍ حتى القطع النهائي).
- منطق الحساب: `عدد النقاط = floor(قيمة الفاتورة ÷ point_value)`, والباقي (fraction) يُضاف إلى `fraction_balance` ويُستخدم في الفاتورة التالية لنفس الزوج (عميل، تاجر) قبل حساب نقاط جديدة.
- Endpoint جديد أو تعديل `/api/wallet/cashback` ليدعم منطق `point_value` لكل تاجر بدل النسبة الثابتة 0.05 الحالية في `AccountingRules.cashbackRate` (القديم يبقى كـ fallback إذا `merchant_profiles.point_value` غير معرّف).
- **صلاحية النقاط (القسم 3 بند 4):** حقل `expires_at` على كل حركة في `points_ledger_merchant` (افتراضي 12 شهرًا من تاريخ المنح، قابل للتخصيص من إعداد أدمن عام أو لكل تاجر). Job دوري (يوميًا) يُعلّم النقاط المنتهية كـ `expired` ويُرسل إشعار مسبق (7 أيام قبل الانتهاء) عبر محرك الإشعارات (الفازة 9).
- **سحب تلقائي عند المرتجعات (القسم 3 بند 6):** Endpoint `POST /api/wallet/refund-deduction` يخصم نفس عدد النقاط الممنوحة عن فاتورة أُلغيت/استُرجعت، بما في ذلك عكس أي كسر رُحِّل بسببها (يُطبَّق بنفس المنطق على طبقة العلامة في الفازة 8).
- **استبدال داخل فروع نفس التاجر (القسم 3 بند 5):** أي استبدال/استخدام نقاط يعمل عبر **كل فروع** نفس `merchant_id` (من الفازة 4) تلقائيًا دون إعداد إضافي — يُختبر فعليًا في الفازة 6 ضمن تدفق QR/POS.
- **توضيح النقاط "العامة" (القسم 3 بند 3):** لا يوجد سجل نقاط "عام" منفصل — `Wallet` (الموجود فعليًا) يبقى هو العرض المجمّع/العام لكل أرصدة التاجر+العلامة معًا، بينما `PointsLedger_Merchant`/`PointsLedger_Brand` هي المصادر التفصيلية، والنقاط "القابلة للتبادل" تُعالَج بمحرك التبادل في الفازة 11.

**المهام (Flutter):**
- امتداد `lib/modules/accounting/accounting_rules.dart` (أو ملف جديد `lib/modules/accounting/merchant_points_rules.dart` أنظف من التعديل المباشر) بدوال:
  - `calculateMerchantPoints(purchaseAmount, pointValue, existingFraction)` → `{points, newFraction}`.
- تحديث شاشة المحفظة الحالية لعرض: الرصيد العام (Wallet) + رصيد كل تاجر منفصل + تاريخ انتهاء صلاحية أقرب دفعة نقاط (أرصدة العلامة تُضاف لاحقًا بالفازة 8، والتبادل بالفازة 11).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ اختبار جديد `test/points_engine_merchant_fraction_test.dart` يغطي أمثلة دقيقة من الوثيقة، مثال:
  - `point_value = 10`, فاتورة = 25 → 2 نقطة + كسر 0.5 يُرحَّل.
  - فاتورة تالية = 12 مع كسر سابق 0.5 → المجموع الفعّال = 12.5 → نقطة واحدة + كسر 2.5؟ (يُحسب بدقة حسب القاعدة: الكسر لا يُقرَّب أبدًا ويُرحَّل حرفيًا).
+ اختبار تراجع (Regression) أن النظام القديم (`cashbackRate`) ما زال يعمل لأي تاجر لم يُفعَّل له `point_value` بعد (لا كسر توافق عكسي).
+ `test/points_expiry_job_test.dart`: نقطة مُنحت قبل 12 شهرًا + يوم → تُعلَّم `expired` بعد تشغيل الـ Job، ولا تُحسب ضمن الرصيد القابل للاستخدام.
+ `test/refund_deduction_test.dart`: محاكاة استرجاع فاتورة سابقة → خصم نفس عدد النقاط الممنوحة (وأي كسر مرتبط) بدقة.

**معيار القبول:** (مطابق لصيغة القسم 15) — "Points Engine (طبقة التاجر) جاهزة عندما تُحتسب النقاط بدقة رياضية 100% مطابقة لمعادلة القسم 4.2 خلال أقل من 5 ثوانٍ من تأكيد العملية، مع ترحيل الكسر بدون أي تقريب، وانتهاء صلاحية تلقائي بعد 12 شهرًا، وخصم صحيح عند أي استرجاع."

---

## الفازة 6 — تدفق QR/POS + دور الكاشير الأساسي (منح نقاط + استبدال بين الفروع)

**المرجع:** القسم 4.12، 6.4، 7.2، القسم 3 (بند 5)، Sprint 1.

**المهام (Backend):**
- Endpoint `POST /api/cashier/grant-points` (يتطلب `cashier_profiles` صالح لنفس الفرع — من الفازة 4) — يستدعي محرك الفازة 5 داخليًا.
- توليد QR Token مشفّر لمرة استخدام واحدة للعميل (القسم 8: "صلاحية مفتوحة" لكود منح النقاط تحديدًا حسب القسم 17).
- التحقق من صلاحيات الكاشير الثابتة فقط (منح نقاط) — أي استدعاء آخر من نفس التوكن يُرفض 403.
- **استبدال/استخدام النقاط عبر كل فروع نفس التاجر (القسم 3 بند 5):** نقاط العميل عند تاجر معيّن (`merchant_id`) صالحة للاستبدال بأي فرع تابع لنفس `merchant_id`، بغض النظر عن الفرع الذي مُنحت منه أصلًا.

**المهام (Flutter):**
- تبويب "وضع الكاشير" يظهر فقط إذا `cashier_profiles` غير فارغة لهذا المستخدم (ضمن نفس تطبيق العميل — **لا تطبيق منفصل**، القسم 4.12).
- شاشة مسح QR لمنح النقاط (لون بنفسجي مميز حسب القسم 14.1).
- شاشة عرض QR الخاص بالعميل (لإظهاره للكاشير).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ اختبار أمني `test/cashier_permissions_test.dart`: محاولة استدعاء أي endpoint غير "منح نقاط" بتوكن كاشير → يجب أن يرجع 403.
+ اختبار ودجت: تبويب الكاشير لا يظهر إطلاقًا لمستخدم بدون `cashier_profiles`.
+ `test/branch_wide_redemption_test.dart`: منح نقاط بفرع (أ) لتاجر معيّن → استبدال ناجح لنفس النقاط بفرع (ب) لنفس التاجر.

**معيار القبول:** "دور الكاشير الأساسي جاهز عندما يقدر الكاشير منح نقاط فقط عبر مسح QR، ويُرفض أي إجراء آخر تلقائيًا بكود 403، ويعمل استبدال النقاط عبر كل فروع نفس التاجر تلقائيًا، دون ظهور أي واجهة كاشير لمستخدم غير مخوَّل."

---

## الفازة 7 — محرك مكافحة الاحتيال (Fraud Control Engine) + سياسة الاحتفاظ بالبيانات

**المرجع:** القسم 4.4، القسم 3 (بند 8، 9)، Sprint 2.

**المهام (Backend):**
- تعديل مسار رفع الفاتورة (`/api/invoices/scan` الحالي) لإضافة تحقق:
  - رفض إن كان تاريخ إصدار الفاتورة أقدم من 48 ساعة من لحظة الرفع.
  - منع تكرار نفس الرقم المرجعي عبر **أي** حساب (فحص عام على الجدول، ليس فقط لنفس المستخدم).
  - حد أقصى لعدد الفواتير يوميًا لكل مستخدم (قابل للتخصيص من إعداد أدمن، قيمة افتراضية معقولة مثل 10).
- جدول `fraud_flags` لتسجيل أي محاولة مرفوضة لمراجعة يدوية لاحقة.
- **كشف التعديل الرقمي للفاتورة (القسم 4.4):** حساب بصمة/تجزئة إدراكية (Perceptual Hash) لكل صورة فاتورة مرفوعة ومقارنتها بصور سابقة؛ عند تطابق مرتفع مع فاتورة سابقة مختلفة البيانات → رفض + تسجيل `fraud_flags` بسبب "تعديل مشتبه به".
- **سياسة الاحتفاظ بالبيانات (القسم 3 بند 9):** إعداد أدمن لمدة الاحتفاظ بصور الفواتير الخام وبيانات الشراء التفصيلية (افتراضي مقترح: 24 شهرًا)، مع Job دوري (شهري) يحذف/يؤرشف الصور الخام والسطور التفصيلية بعد انتهاء المدة، مع الإبقاء على السجلات المالية المجمّعة (نقاط، مبالغ) دون حذف.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ `test/fraud_control_rules_test.dart` (منطق Dart إن وُجد على العميل) + سكربت اختبار Backend (Node أو curl) يغطي 3 سيناريوهات من كتالوج القسم 9:
  - رفع نفس رقم فاتورة مرجعي مرتين لنفس الحساب → رفض فوري.
  - نفس الرقم من حسابين مختلفين → رفض الثانية + تسجيل `fraud_flags`.
  - فاتورة بتاريخ إصدار أقدم من 48 ساعة → رفض.
+ `test/invoice_image_fingerprint_test.dart`: رفع نفس صورة معدّلة (اقتصاص/تعديل طفيف) ببيانات مختلفة → رفض + `fraud_flags`.
+ `test/data_retention_purge_job_test.dart`: فاتورة أقدم من المدة المُعدَّة → تُحذف صورتها الخام بعد تشغيل الـ Job، وتبقى السجلات المالية المجمّعة سليمة.

**معيار القبول:** "Fraud Control جاهز عندما تُرفض 100% من محاولات إعادة استخدام نفس الرقم المرجعي عبر أي حساب، وتُرفض كل فاتورة تتجاوز نافذة 48 ساعة، وتُكتشف الصور المُعدَّلة رقميًا، وتُحذف البيانات الخام تلقائيًا بعد مدة الاحتفاظ المُعدَّة."

---

## الفازة 8 — التعرف السطري على الفاتورة + دليل المنتجات + مطابقة العلامات (Brand Layer)

**المرجع:** القسم 4.1، 4.2 (جزء العلامة)، 6.3، Sprint 3.

**المهام (Backend):**
- جدول `product_registry` (كل علامة تجارية ترفع منتجاتها: اسم، صورة، باركود).
- جدول `invoice_line_items` (سطر لكل منتج داخل الفاتورة: اسم، كمية، سعر).
- جدول `brand_matches` (نتيجة مطابقة سطر الفاتورة مع `product_registry`).
- توسيع محرك OCR الحالي (`lib/screens/scan_invoice_screen.dart` + المحرك الخلفي المذكور في `/memories/repo/kupuna-ops.md`: ML Kit + Tesseract) لاستخراج الأسطر لا الإجمالي فقط. **هذا امتداد على المحرك الموجود فعليًا، وليس بناء من الصفر.**
- `customer_brand_fraction_balance` + `points_ledger_brand` (نفس نمط الفازة 5 تمامًا، لكن مستقل بالكامل — سجلّان منفصلان دائمًا كما تنص الوثيقة). نفس آلية صلاحية النقاط (12 شهرًا + إشعار مسبق) وسحب المرتجعات (القسم 3 بند 4، 6) تُطبَّق طبق الأصل هنا.
- آلة حالة الفاتورة الكاملة (القسم 4.1): `uploaded → processing → {approved | rejected → disputed? → dispute_upheld/denied}`، **يُمنع** الانتقال المباشر `processing → disputed`.
- **سياسة الاحتفاظ (تكامل مع الفازة 7):** صور/سطور الفواتير الجديدة تُنشأ مباشرة بحقل `retention_expires_at` حسب الإعداد العام.

**المهام (Flutter):**
- شاشات إضافية للوحة العلامة التجارية (القسم 6.3): خريطة التوزيع الجغرافي (أماكن ظهور المنتج بفواتير مؤكدة)، تحليل شريحة المستهلكين (ديموغرافي)، ولوحة "دعم المحلات الأكثر مبيعًا" (ترتيب المحلات حسب حجم المبيعات + حملات دعم مشترك).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ اختبار امتداد لمعيار الدقة الحالي: راجع/وسّع `test/invoice_parser_accuracy_20_samples_test.dart` (موجود مسبقًا بنسبة دقة 100% على مستوى النص) ليشمل استخراج الأسطر لا الإجمالي فقط.
+ `test/invoice_state_machine_test.dart` يتحقق أن `processing → disputed` مباشرة **مرفوض برمجيًا** (استثناء/خطأ).
+ `test/brand_points_engine_test.dart` بنفس منطق اختبار الفازة 5 لكن لسجل العلامة المنفصل.
+ `test/brand_points_expiry_and_refund_test.dart`: تكرار اختبارَي انتهاء الصلاحية وخصم المرتجعات من الفازة 5 على طبقة العلامة.

**معيار القبول:** "محرك التعرف السطري جاهز عندما يُنشئ `BrandMatch` صحيح لكل سطر مطابق، وتُحتسب نقاط العلامة من مجموع الأسطر المطابقة فقط لا إجمالي الفاتورة، مع بقاء سجلّي نقاط التاجر والعلامة منفصلين تمامًا، ونفس قواعد الصلاحية والاسترجاع مطبّقة على مستوى العلامة."

---

## الفازة 9 — محرك المجتمعات + العروض + الإشعارات

**المرجع:** القسم 4.3، 4.5، 4.9، 5.4، Sprint 4.

**المهام (Backend):**
- امتداد جداول `groups`/`group_messages` الحالية (موجودة فعليًا في `backend/server.js`) بإضافة: قروب تلقائي لكل `merchant_profiles`/`brand_profiles` عند التفعيل، وانضمام تلقائي للعميل عند أول فاتورة `approved` من ذلك التاجر/العلامة (Trigger بعد نجاح الفازة 5/8).
- صلاحيات إدارة القروب (تثبيت رسالة، حذف رسالة، حظر عضو) — تحقق من الصلاحية على مستوى Endpoint.
- امتداد جدول `offers` الحالي بحقول الاستهداف (الكل/حد نقاط/شريحة).
- ربط Firebase Cloud Messaging (FCM) فعليًا (القسم 12) لإشعارات: تأكيد نقاط، عرض مستهدف، انتهاء صلاحية نقاط، قبول/رفض فاتورة، رسالة قروب جديدة.

**المهام (Flutter):**
- تحديث `lib/widgets/map_bar.dart`/`lib/screens/offers_screen.dart` (موجودان) لدعم الاستهداف الجديد بدون كسر الوضع `embedded` الحالي.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ اختبار تكامل: محاكاة أول فاتورة `approved` من تاجر → تحقق أن العميل انضم تلقائيًا لقروب ذلك التاجر (استعلام `group_memberships`).
+ اختبار إشعار: إرسال إشعار تجريبي عبر FCM Test Token والتأكد من وصوله (يدويًا أو عبر سكربت).

**معيار القبول:** "محرك المجتمعات جاهز عندما ينضم العميل تلقائيًا لقروب أي تاجر/علامة فور قبول أول فاتورة له منهم، دون أي إجراء يدوي."

---

## الفازة 10 — محرك البلاغات والاعتراضات (Reports & Disputes)

**المرجع:** القسم 4.10، 5.5، Sprint 5.

**المهام (Backend):**
- جدول `reports` (`thank_you_sent_at`, `reward_granted`, `reward_type`, تصنيف: منتج علامة / خدمة كاشير).
- آلة الحالة: `new → under_review → {accepted → [اختياري] reward_granted → closed | rejected → closed}`.
- منطق التوجيه: بلاغ منتج علامة → يظهر لصاحب المحل وصاحب العلامة معًا؛ بلاغ خدمة كاشير → لصاحب المحل فقط.
- رسالة شكر فورية تلقائية (تُسجَّل `thank_you_sent_at` فور الإنشاء، قبل أي معالجة).
- جدول/حقل `Dispute` مرتبط بآلة حالة الفاتورة من الفازة 8 (لا اعتراض إلا من حالة `rejected`).
- SLA زمني قابل للتخصيص من الأدمن (افتراضي 72 ساعة) لإغلاق الاعتراض.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ `test/reports_engine_state_machine_test.dart` يغطي كل الانتقالات + التحقق من التوجيه الصحيح حسب نوع البلاغ.
+ اختبار توقيت: `thank_you_sent_at` يُسجَّل فور الإنشاء (قبل `under_review`).

**معيار القبول:** "محرك البلاغات جاهز عندما تصل رسالة الشكر فوريًا عند كل بلاغ جديد، ويصل التوجيه للطرف الصحيح 100% من الحالات حسب نوع البلاغ."

---

## الفازة 11 — الضمان/التسوية + تبادل النقاط + سوق المكافآت + استلام عبر الكاشير

**المرجع:** القسم 4.6، 4.8، 4.12، 5.2، Sprint 6.

**المهام (Backend):**
- `escrow_accounts` + `settlements` (إيداع مسبق من التاجر/العلامة، صرف تلقائي عند الاستخدام، رد عند انتهاء الصلاحية).
- `exchange_rate_config` + `exchange_transactions`: معدل التبادل تحدده **إدارة كوبونا فقط**، بمعادلة القسم 4.6 بالضبط:
  `عدد النقاط الناتجة = (عدد النقاط المُحوَّلة × قيمة نقطة المصدر) ÷ قيمة نقطة الوجهة`
- `reward_claims` (`points_deducted_at` وقت الاستبدال داخل التطبيق — **ليس** وقت مسح الكاشير، `pickup_qr_code` صالح 24 ساعة فقط لهذا الكود تحديدًا، `status`).
- **Job مجدوَل كل ساعة على الأقل** يفحص `reward_claims` منتهية الصلاحية ويُعيد النقاط تلقائيًا (`refunded_as_points`) دون تدخل يدوي.
- Endpoint استلام الكاشير للمكافأة المادية (`redeemed`) — يتحقق فقط من صلاحية الكود، **لا يخصم نقاط** (الخصم حدث مسبقًا).
- **قفل على مستوى قاعدة البيانات (Row-Level Lock)** لمنع استخدام نفس كود الاستلام مرتين في نفس اللحظة (أول من يصل يفوز، القسم 9).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ `test/points_exchange_rate_test.dart`: مثال الوثيقة بالحرف — نقطة تاجر (أ) = 10 دنانير، نقطة تاجر (ب) = 5 دنانير → تحويل نقطة واحدة من (أ) يعطي نقطتين عند (ب).
+ `test/reward_claim_expiry_job_test.dart`: محاكاة مرور 24 ساعة + تشغيل الـ Job → تحقق `status = refunded_as_points` تلقائيًا.
+ اختبار تزامن (Concurrency): محاولتا استلام لنفس الكود بنفس اللحظة → واحدة فقط تنجح (`redeemed`)، الثانية تُرفض فورًا.

**معيار القبول:** "محرك التسوية والتبادل جاهز عندما تُطابق معادلة التبادل مثال الوثيقة تمامًا، وتُسترجع كل مكافأة منتهية الصلاحية تلقائيًا خلال ساعة واحدة كحد أقصى من انتهائها، ولا يمكن لأي كود استلام أن يُستخدم مرتين."

---

## الفازة 12 — إعلانات الأفراد والتوريد (Peer Ads & Sourcing)

**المرجع:** القسم 4.11، 5.6، Sprint 7.

**المهام (Backend):**
- `peer_ads` (`owner_user_id`, `content`, `target_type` ['group'|'merchant_category'|'geo_region'], `target_value`, `fee_paid`, `status`).
- آلة الحالة: `draft → pending_admin_review → {approved → [بعد الدفع] active → expired | rejected (سبب إلزامي)}`.
- `sourcing_inquiries` (تاجر يبدأ محادثة توريد مستقلة): `opened → in_discussion → {closed_agreed | closed_no_deal}` (توثيق يدوي فقط، خارج محركات النقاط/الضمان).
- Endpoint مراجعة أدمن لإعلانات الأفراد قبل النشر.

**المهام (Flutter):**
- تبويب "إعلانات الأفراد" في الرئيسية (القسم 6.1) + شاشة إنشاء إعلان (محتوى + استهداف + مدة + دفع).
- شارة واضحة تميّز "إعلان من فرد" عن عرض رسمي (القسم 14.3).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ `test/peer_ad_state_machine_test.dart` يغطي كل انتقالات الحالة، بما فيها رفض بدون سبب (يجب أن يفشل التحقق — السبب إلزامي).

**معيار القبول:** "محرك إعلانات الأفراد جاهز عندما يُمنع نشر أي إعلان قبل موافقة الأدمن والدفع معًا، ويظهر سبب الرفض دائمًا للعميل عند الرفض."

---

## الفازة 13 — محرك إدارة الأدوار والاشتراكات الكامل

**المرجع:** القسم 4.13، 2.3–2.5 (تفصيل كامل)، Sprint 8.

**المهام:**
- استكمال ما بدأ بالفازة 3: تحكم الأدمن الكامل بمدة التجربة لكل حالة على حدة (30-90 يوم)، دورة الفوترة الفعلية، **تأكيد الدفع عبر Webhook إلزامي قبل أي تفعيل** (القسم 9: "لا تفعيل مبدئي متفائل").
- تمديد يدوي من لوحة الأدمن لاشتراك معيّن عند الحاجة.
- فصل تام موثّق بالاختبار بين حالة الدور المدفوع وحالة العميل المجاني على نفس الحساب (تكرار اختبار الفازة 3 بسيناريوهات فوترة حقيقية أكثر).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ `test/payment_webhook_activation_test.dart`: محاولة تفعيل اشتراك بدون تأكيد Webhook ناجح → يجب أن تفشل (لا تفعيل متفائل).

**معيار القبول:** "إدارة الاشتراكات جاهزة عندما لا يحدث أي تفعيل فعلي إلا بعد Webhook تأكيد دفع ناجح موثّق، وأن أي تمديد يدوي من الأدمن ينعكس فورًا بالحالة الصحيحة."

---

## الفازة 14 — محرك التنبؤ بالسلوك + Loyalty Health Score

**المرجع:** القسم 4.7، 6.2 (Loyalty Health Score)، Sprint 9.

**المهام:**
- محرك Rule-Based أولاً (تحليل تكرار/توقيت/فئة الشراء) لإرسال عرض استباقي — **ممنوع البدء بـ ML قبل توفر بيانات كافية**، هذا نص صريح بالوثيقة.
- حساب Loyalty Health Score (رقم + اتجاه) لعرضه في الرئيسية للتاجر (القسم 6.2)، بتصور بصري (عداد دائري/مؤشر ملون وليس رقمًا مجردًا — القسم 14.3).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ اختبارات قواعد Rule-Based بأمثلة بيانات شراء وهمية (نمط تكرار عالي/منخفض) والتحقق من نتيجة العرض الاستباقي المتوقعة.
+ اختبار ودجت لعرض المؤشر الدائري.

**معيار القبول:** "المحرك التنبؤي جاهز عندما يُنتج توصية عرض استباقي صحيحة منطقيًا لكل نمط شراء اختباري معرّف مسبقًا، دون أي استدعاء لنموذج تعلّم آلي في هذه المرحلة."

---

## الفازة 15 — مصفوفة الصلاحيات والأمان الشامل (تدقيق وإنفاذ)

**المرجع:** القسم 7 (كامل)، القسم 8 (كامل).

**المهام:**
- **تدقيق (لا إنشاء من جديد):** التحقق أن نظام صلاحيات مدير الفرع (أُنشئ في الفازة 4) ما زال يطبّق `false` افتراضيًا لكل صلاحية جديدة، وأن صلاحيات فريق العلامة التجارية (القسم 7.3) مطبَّقة بنفس المبدأ.
- إنفاذ صلاحيات الكاشير الثابتة (تكرار/تعزيز اختبار الفازة 6) على **كل** Endpoint جديد أُضيف في الفازات 7-14، ليس فقط منح النقاط.
- تدقيق أمني شامل:
  - Hashing: bcrypt/Argon2 فقط، ممنوع نص صريح (تحقق من الكود الحالي — يبدو أن `bcryptjs` مستخدم فعليًا في `backend/server.js`، وثّق ذلك).
  - JWT + Refresh Token، إبطال فوري عند تغيير الرقم السري.
  - أكواد QR: Token مشفّر لمرة استخدام واحدة، غير قابل للتخمين أو الإعادة (تدقيق كل أكواد QR المُضافة في الفازات 6 و11).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ `test/branch_manager_permissions_enforcement_test.dart`: التحقق أن كل Endpoint جديد (الفازات 7-14) يرفض أي طلب من مدير فرع بدون الصلاحية المحددة بكود 403.
+ سكربت أمني: محاولة استخدام QR Token مرتين → رفض الثانية دائمًا (تكرار اختبار الفازة 11 بنطاق أوسع).
+ سكربت أمني: محاولة إبطال جلسة (Token) بعد تغيير الرقم السري → التحقق من رفض الطلبات اللاحقة بنفس التوكن القديم.

**معيار القبول:** "طبقة الصلاحيات والأمان جاهزة عندما تكون كل الصلاحيات الجديدة `false` افتراضيًا، وتُرفض أي محاولة تجاوز صلاحية بكود 403 في 100% من نقاط الدخول المُختبرة عبر كل الفازات."

---

## الفازة 16 — كتالوج الأخطاء والحواف + لوحة الأدمن الموحّدة + تدقيق التصميم/i18n

**المرجع:** القسم 9 (كامل)، القسم 6.5، القسم 14 (كامل).

**المهام:**
- تنفيذ/تأكيد كل سطر في جدول القسم 9 صراحة كاختبار مستقل (أغلبها بُني ضمنيًا بالفازات السابقة — هذه فازة **تجميع وتغطية شاملة**، لا بناء جديد بالكامل):
  1. فشل الإنترنت أثناء مسح QR → Offline Queue محلي + إعادة محاولة تلقائية.
  2. تعارض استخدام كود استلام متزامن → أول من يصل يفوز (مُغطّى بالفازة 11، يُعاد اختباره هنا ضمن السيناريو الكامل).
  3. تكرار رفع نفس صورة الفاتورة → رفض فوري (مُغطّى بالفازة 7).
  4. نفس الرقم المرجعي من حسابين → رفض + تسجيل احتيال (مُغطّى بالفازة 7).
  5. انتهاء التجربة أثناء استخدام فعلي → لا قطع مفاجئ، تنبيه + انتقال سلس لـ`grace_period` (مُغطّى بالفازة 3/13).
  6. فشل بوابة الدفع أثناء التفعيل → لا تفعيل متفائل (مُغطّى بالفازة 13).
- لوحة إدارة كوبونا الموحّدة (القسم 6.5): تجميع كل شاشات الأدمن المتفرقة من الفازات السابقة في مكان واحد متسق (طلبات عضوية، عمليات حية، إعدادات المحركات، إعدادات التجربة، إدارة الاشتراكات، التسويات، سجلات الاحتيال/البلاغات، مراجعة إعلانات الأفراد، الدعم الفني).
- تدقيق i18n نهائي: فحص شامل (grep) لأي نص عربي/إنجليزي مباشر متبقٍ خارج `.tr()` في كل الشاشات المُضافة بالفازات 1-15.
- تدقيق تصميم: الألوان حسب الدور (القسم 14.1)، نمط التنقل (القسم 14.2)، RTL/LTR الكامل.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ `test/edge_cases_full_catalog_test.dart`: اختبار واحد شامل يمر على كل سطر من جدول القسم 9 بالترتيب.
+ فحص نصي: بحث Regex عن حروف عربية مباشرة داخل `Text(` في كل ملفات `lib/screens/*.dart` و`lib/widgets/*.dart` المُضافة حديثًا — يجب أن يرجع صفر نتائج خارج التعليقات/البيانات التجريبية.

**معيار القبول:** "هذه الفازة جاهزة عندما يمر كل سطر من كتالوج القسم 9 باختبار مستقل ناجح، ولا يتبقى أي نص واجهة غير مُترجَم في أي شاشة جديدة."

---

## الفازة 17 — تدقيق شامل نهائي (End-to-End) والتسليم

**المرجع:** القسم 15، القسم 17 (ملخص القرارات النهائية).

**المهام:**
- سيناريو اختبار واحد متكامل يمر بالسلسلة الكاملة:
  `تسجيل عميل جديد → تفعيل دور تاجر → موافقة أدمن → دفع/تجربة → مسح فاتورة → قبول → نقاط تاجر+علامة → انضمام تلقائي للقروب → إنشاء عرض → استخدامه → تقديم بلاغ → رد بمكافأة → تبادل نقاط → استبدال بمكافأة مادية → استلام عبر كاشير`.
- مراجعة نهائية مقابل **جدول القسم 17 كاملًا**، بندًا بندًا، والتأكد أن كل قيمة معتمدة (48 ساعة، 24 ساعة، 30-90 يوم، 7 أيام سماح، إلخ) مُطبَّقة حرفيًا في الكود لا تقريبًا.
- تقرير تسليم نهائي `KUPUNA_MASTER_SPEC_DELIVERY_REPORT.md` يوثّق كل فازة، تاريخ إنجازها، ونتيجة اختبارها.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ سكربت/اختبار تكامل واحد يغطي السيناريو الكامل أعلاه من طرف لطرف (End-to-End)، ونجاحه شرط وحيد لإغلاق الفازة.
+ مراجعة يدوية نهائية لجدول القسم 17 كقائمة تحقق (Checklist) داخل تقرير التسليم.

**معيار القبول:** "المشروع جاهز للتسليم عندما ينجح سيناريو End-to-End الكامل بدون أي تدخل يدوي غير موصوف في الوثيقة، وتتطابق كل قيمة في جدول القسم 17 مع القيمة الفعلية في الكود."

---

# الجزء الثاني: فازات نظام التصميم البصري الكامل (القسم 18 من `docs/KUPUNA_MASTER_SPEC.md`)

> **سياق مهم قبل البدء بهذه الفازات:** الفازات 0–18 أعلاه أنتجت منطق العمل (Backend + Flutter functional) وتم التحقق منها فعليًا (E2E ضد الإنتاج). لكن **الطبقة البصرية الحالية لا تتبع أي نظام تصميم موحّد** — تدقيق مباشر للكود يُظهر أن كل الشاشات تستخدم حاليًا `Colors.deepPurple` (لون افتراضي عام في `lib/main.dart` وعشرات الشاشات)، ولا يوجد أي ملف `design_tokens`/`theme` مخصص بالمستودع حتى تاريخه. الفازات 19–25 هي طبقة **إعادة تصميم بصري شاملة** تُطبَّق فوق الوظائف الموجودة، دون تغيير أي منطق عمل أو Endpoint.
>
> **قاعدة إلزامية إضافية تحكم هذا الجزء بالكامل:**
> 1. **ممنوع كتابة قيم Hex أو مقاسات مباشرة متفرقة داخل الشاشات.** كل لون/خط/قياس يُستدعى حصريًا من ملفات `design tokens` المركزية (الفازة 19) — أي قيمة Hex ظاهرة مباشرة في widget بعد الفازة 19 تُعتبر خطأ يفشل به أمر التحقق.
> 2. **لا حذف وظيفي.** إعادة التصميم تستبدل الشكل البصري (ألوان/خطوط/زوايا/تخطيط) فقط، ولا تحذف أو تُعطّل أي منطق/Endpoint/State يعمل حاليًا. أي شاشة تُعاد تصميمها يجب أن تمر بنفس اختبارات الودجت (إن وُجدت) بعد التعديل.
> 3. **الترتيب صارم هنا أيضًا:** لا تُطبَّق الفازة 21 (شاشات العميل) قبل اكتمال الفازة 19 (Tokens) والفازة 20 (مكتبة المكوّنات) بنجاح 100%، لأن كل الشاشات تعتمد على مكوّنات وألوان مركزية واحدة.
> 4. **فحص اختلاط الألوان بين الأوضاع (القسم 18-ز) إلزامي بعد كل فازة من 21 إلى 24**، وليس فقط في الفازة 25 النهائية — أي ظهور لـ `--teal`/`--mint` داخل لوحة تاجر/علامة، أو `--indigo` داخل تطبيق العميل، أو أي لون غير `--violet` بوضع الكاشير، يُعتبر فشلًا فوريًا لأمر التحقق.

---

## الفازة 19 — البنية التحتية لنظام التصميم (Design Tokens + الخطوط + الثيمات حسب الدور)

**المرجع:** القسم 18-أ (كامل).

**المهام (Flutter):**
- ملف جديد `lib/theme/design_tokens.dart`: كل قيم القسم 18-أ.1 كثوابت `Color` باسم مطابق (`kInk`, `kSand`, `kWhite`, `kGold`, `kTeal`, `kTealDark`, `kMint`, `kIndigo`, `kIndigoLight`, `kViolet`, `kLine`, `kLineDark`) — لا قيم Hex مكتوبة بأي مكان آخر بعد هذه الفازة.
- ملف جديد `lib/theme/design_radii.dart` و/أو ثوابت ضمن نفس ملف Tokens لقيم القسم 18-أ.4 (`kRadiusCard = 16`, `kRadiusCardLarge = 18`, `kRadiusPill = 100`, `kRadiusHeaderBottom = 26`, `kRadiusMerchantPanel = 26`, إلخ) وقيم القسم 18-أ.5 للظلال (`kShadowFloating`).
- إضافة خطوط `Cairo` و`IBM Plex Sans Arabic` عبر حزمة `google_fonts` (الأفضل لتفادي تضخيم حجم التطبيق بخطوط محلية لـ16 لغة) أو كخطوط أصول محلية إن تعذّر الوصول لـ Google Fonts CDN وقت التشغيل — القرار يُوثَّق في تقرير الفازة. إنشاء `TextStyle` جاهزة: `kDisplayTextStyle` (Cairo 700/800/900) و`kBodyTextStyle` (IBM Plex Sans Arabic 300/400/500/600)، ودالة مساعدة `kPointsNumberStyle` (Cairo، وزن 800 كحد أدنى دائمًا — تُستخدم لكل رقم نقاط/مبلغ في التطبيق بلا استثناء حسب القسم 18-أ.3).
- ملف جديد `lib/theme/app_themes.dart`: 4 كائنات `ThemeData` منفصلة — `customerTheme` (أساسه `--teal`/`--sand`)، `merchantBrandTheme` (أساسه `--indigo`)، `adminTheme` (أساسه `--ink`، Dark Mode)، `cashierTheme` (أساسه `--violet`). كل ثيم يستخدم حصريًا ثوابت `design_tokens.dart`.
- تعديل `lib/main.dart`: إزالة `ThemeData(primarySwatch: Colors.deepPurple)` الحالي، واستبداله بآلية اختيار الثيم بناءً على `_activeRole` الحالي (تُقرأ من `AppSession` الموجودة فعليًا) — لا يزال هذا التبديل الفعلي للشاشات يحدث بالفازة 21+ بحسب كل شاشة، لكن **البنية التحتية للثيمات يجب أن تكون جاهزة ومربوطة هنا أولًا**.
- **لا تعديل على أي شاشة أخرى في هذه الفازة** — فازة بنية تحتية بحتة، تمامًا بنفس فلسفة الفازة 1 (بيانات فقط، بدون شاشات).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ فحص نصي (grep/Select-String) عبر `lib/theme/*.dart`: التأكد أن كل الـ 12 لونًا من القسم 18-أ.1 معرّفة بقيمة Hex مطابقة حرفيًا (`#16241F`, `#F3F1E8`, `#FFFEFB`, `#D9A441`, `#1B7A6B`, `#12594E`, `#7FD9C4`, `#263859`, `#34496E`, `#6C3FA8`) — أي فرق قيمة يُعتبر فشلًا.
+ اختبار ودجت جديد `test/design_tokens_test.dart`: يبني كل `ThemeData` الأربعة ويتحقق أن `colorScheme`/`scaffoldBackgroundColor` لكل ثيم يطابق اللون المعتمد لوضعه (مثال: `adminTheme.scaffoldBackgroundColor == kInk`).

**معيار القبول:** "البنية التحتية لنظام التصميم جاهزة عندما تكون كل الألوان/الخطوط/القياسات المعتمدة موجودة كثوابت مركزية بقيم مطابقة حرفيًا للقسم 18-أ، وتُبنى 4 ثيمات كاملة (عميل/تاجر-علامة/أدمن/كاشير) بدون أي قيمة Hex مباشرة متفرقة، مع بقاء كل الشاشات الحالية تعمل دون كسر (`flutter analyze`/`flutter test` نظيفين)."

---

## الفازة 20 — مكتبة المكوّنات المشتركة (Shared Component Library)

**المرجع:** القسم 18-ب (كامل).

**المهام (Flutter):**
جميع الملفات الجديدة تحت `lib/widgets/design_system/`، وكلها تستخدم حصريًا ثوابت `design_tokens.dart` من الفازة 19:
- `kupuna_bottom_navbar.dart` — تطبيق القسم 18-ب.1 (5 عناصر، تبويب نشط `--teal` وزن 700، غير نشط شفافية 50%).
- `kupuna_top_tabs.dart` — تطبيق القسم 18-ب.2 (Pill tabs، نشط خلفية `--ink` نص أبيض).
- `kupuna_offer_card.dart` — تطبيق القسم 18-ب.3 بالكامل بما فيها منطق شارة المصدر الإلزامية (`enum OfferSourceBadge { none, brand, peer }` → تُحسَب من بيانات العرض نفسها، لا تُترك اختيارية أبدًا للمستدعي).
- `kupuna_chat_bubble.dart` — تطبيق القسم 18-ب.4 (فقاعتان: مستخدم حالي/طرف آخر، زوايا معكوسة، صورة رمزية بلون حسب نوع المرسل).
- `kupuna_dual_wallet_rings.dart` — تطبيق القسم 18-ب.5 بالكامل (حلقتان متداخلتان + رقم مركزي Cairo 800/24px + Legend سفلي).
- `kupuna_loyalty_health_ring.dart` — تطبيق القسم 18-ب.6 (Conic Gradient، الجزء المُنجَز `--gold` حصرًا) — **يُمنع برمجيًا** وجود أي طريقة عرض بديلة لهذا المؤشر كرقم مجرد (لا `Text`-only variant يُصدَّر من هذا الملف).
- `kupuna_cashier_mode_screen_wrapper.dart` — تطبيق القسم 18-ب.7 (خلفية `--violet` صلبة كاملة الشاشة، شارة علوية، مربع مسح، زرّان).
- `kupuna_status_pill.dart` — تطبيق القسم 18-ب.8 (3 حالات: معلّقة ذهبي / مقبولة تيل-أو-نعناعي / مرفوضة رمادي، حسب `enum StatusPillKind`).
- استبدال أي استخدام حالي لبطاقة عرض/فقاعة دردشة/شارة حالة "يدوية" (Widget مكتوب Inline بشاشة معينة) بهذه المكوّنات المشتركة الجديدة **فقط في الملفات التي سبق فيها استخدام غير موحّد بشكل بسيط** (مثال: أي `Container` يمثّل فقاعة دردشة في `community_screen.dart` يُستبدل بـ`KupunaChatBubble`) — الاستبدال الكامل عبر كل الشاشات يحدث فعليًا ضمن الفازات 21-24 التالية؛ هذه الفازة تبني المكوّنات وتربطها في ملف عرض توضيحي واحد فقط.
- ملف توضيحي (اختياري لكن مُستحسَن) `lib/widgets/design_system/_gallery_screen.dart` يعرض كل المكوّنات الثمانية أعلاه بحالاتها المختلفة، لتسهيل المراجعة البصرية السريعة قبل تعميمها على الشاشات الحقيقية.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ اختبار ودجت مستقل لكل مكوّن (8 ملفات `test/design_system/kupuna_*_test.dart` على الأقل): يتحقق من الألوان الصحيحة حسب الحالة (مثال: `KupunaOfferCard` مع `source: peer` يعرض شارة نصها "فرد" بخلفية `kGold`؛ مع `source: brand` خلفية `kIndigo`؛ مع `source: none` لا شارة إطلاقًا).
+ اختبار خاص لـ `KupunaLoyaltyHealthRing`: التأكد أن الودجت لا يحتوي مسارًا لعرض الرقم بدون الدائرة (فحص شجرة الودجت `find.byType` يجب أن يجد `CustomPaint`/`Container` دائري أبًا مباشرًا لأي `Text` رقمي).

**معيار القبول:** "مكتبة المكوّنات جاهزة عندما تُبنى كل الودجتات الثمانية المطلوبة، تلتزم 100% بألوان/قياسات القسم 18-ب دون أي قيمة مكتوبة يدويًا خارج `design_tokens.dart`، وتغطيها اختبارات ودجت مستقلة ناجحة."

---

## الفازة 21 — تطبيق نظام التصميم على شاشات تطبيق العميل

**المرجع:** القسم 18-ج (كامل، الشاشات ج.1 إلى ج.13).

**المهام (Flutter):** تحديث كل شاشة عميل موجودة فعليًا في `lib/screens/` (لا إنشاء من الصفر، الشاشات موجودة وظيفيًا من فازات سابقة) لتستخدم `customerTheme` + مكوّنات الفازة 20 حصرًا:
- `onboarding_screen.dart` / `splash_screen.dart` ← القسم 18-ج.1 (تدرّج `--teal` → `--teal-dark`، شبكة 2×2).
- `home_screen.dart` / `home_content_screen.dart` / `home_page_content.dart` ← القسم 18-ج.2 (رأس منحني `--teal`، تبويبات أفقية، قائمة `KupunaOfferCard`).
- `offers_screen.dart` / `offers_list_screen.dart` / `offer_detail_screen.dart` ← القسم 18-ج.3.
- تبويب/شاشة إعلانات الأفراد (المُطبَّقة بالفازة 12) ← القسم 18-ج.4 (شارة "فرد" إلزامية دون استثناء واحد).
- `scan_invoice_screen.dart` ← القسم 18-ج.5 (زر QR دائري 120px بارز + رابط رفع صورة أصغر وأقل تباينًا).
- `wallet_engine_screen.dart` / `points_screen.dart` ← القسم 18-ج.6 (استخدام `KupunaDualWalletRings` إلزاميًا في الرأس).
- `rewards_screen.dart` / `my_rewards_screen.dart` / `reward_qr_code_screen.dart` ← القسم 18-ج.7 (تبويبا رقمي/مادي، تنبيه اقتراب انتهاء صلاحية كود الاستلام).
- `community_screen.dart` ← القسم 18-ج.8 (استخدام `KupunaChatBubble`، تثبيت إعلانات الأفراد بإطار ذهبي).
- `report_screen.dart` / `report_issue_screen.dart` ← القسم 18-ج.9 (Toast تأكيد فوري، أزرار اختيار 3 أنواع بلاغ).
- شاشة إنشاء إعلان فردي (من الفازة 12) ← القسم 18-ج.10.
- `cashier_dashboard_screen.dart` (نقطة الدخول من تطبيق العميل) ← القسم 18-ج.11 عبر `KupunaCashierModeScreenWrapper`.
- `my_roles_screen.dart` ← القسم 18-ج.12 (بطاقة عميل ثابتة أولًا بحد `--teal`، بطاقات أدوار أخرى بحدود حسب اللون، زر التبديل الثابت بأعلى الشاشة).
- `profile_screen.dart` ← القسم 18-ج.13.
- إزالة كل استخدامات `Colors.deepPurple`/أي لون مكتوب مباشرة من هذه الشاشات تحديدًا (استكمال باقي الشاشات في الفازات التالية إن وُجدت هناك).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ فحص نصي: `Select-String -Pattern "Colors\.deepPurple|Color\(0xFF" -Path lib/screens/*.dart` على كل الملفات المذكورة أعلاه يجب أن يرجع **صفر نتائج**.
+ فحص عدم اختلاط الألوان (القسم 18-ز): بحث عن `kIndigo|kViolet` داخل نفس الملفات أعلاه → صفر نتائج (تطبيق العميل لا يستخدم ألوان لوحات أخرى، عدا شاشة الكاشير التي هي استثناء موثّق بالتصميم نفسه).
+ اختبار ودجت واحد على الأقل محدَّث لكل شاشة رئيسية (`home_screen_test.dart`, `wallet_engine_screen_test.dart`, إلخ) يتحقق من ظهور المكوّن الصحيح (مثال: وجود `KupunaDualWalletRings` بشاشة المحفظة).

**معيار القبول:** "شاشات العميل جاهزة عندما تلتزم كل شاشة مذكورة أعلاه حرفيًا بألوان/مكوّنات القسم 18-ج، ولا يتبقى أي لون Hex مكتوب مباشرة أو أي تسرّب للون `--indigo`/`--violet` خارج شاشة الكاشير الموثّقة صراحة."

---

## الفازة 22 — تطبيق نظام التصميم على لوحة تحكم التاجر

**المرجع:** القسم 18-د (كامل، الشاشات د.1 إلى د.11).

**المهام (Flutter):** تحديث `merchant_dashboard_screen.dart` وكل الشاشات الفرعية المبنية بالفازات 3، 4، 5، 6، 9، 10، 11، 14 (إدارة الفروع، مراجعة الفواتير، العروض، إدارة القروب، الضمان والتسويات، صندوق البلاغات، إدارة الموظفين، مراقبة إعلانات الأفراد، التقارير) لتستخدم `merchantBrandTheme` + مكوّنات الفازة 20 حصرًا:
- كل خلفية أساسية `--indigo`، كل بطاقة داخلية `--indigo-light` أو شفافية بيضاء 6-8%.
- تطبيق القسم 18-د.1 (استثناء `--sand` لشاشة التسجيل قبل التفعيل فقط) إلى القسم 18-د.11 حرفيًا لكل شاشة.
- استخدام `KupunaLoyaltyHealthRing` إلزاميًا في الرئيسية (لا عرض رقمي بديل بأي حال).
- استخدام `KupunaStatusPill` في كل قوائم المراجعة (فواتير، بلاغات).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ فحص نصي: صفر نتائج لـ `Colors.deepPurple`/Hex مباشر في كل شاشات لوحة التاجر.
+ فحص عدم اختلاط الألوان: بحث عن `kTeal|kMint` داخل ملفات لوحة التاجر (باستثناء الاستخدام الحصري المسموح به داخل `KupunaLoyaltyHealthRing` نفسه فقط، وهو `kGold` لا `kTeal`/`kMint`) → صفر نتائج.
+ اختبار ودجت: التأكد أن `merchant_dashboard_screen.dart` لا يحتوي إطلاقًا على `kTeal`/`kMint` في شجرة الودجت الرئيسية.

**معيار القبول:** "لوحة التاجر جاهزة بصريًا عندما تكون كل شاشاتها المذكورة بخلفية `--indigo` كاملة بلا أي تسرّب للون `--teal`/`--mint`، ومؤشر صحة الولاء معروض كدائرة إلزامية لا رقم مجرد."

---

## الفازة 23 — تطبيق نظام التصميم على لوحة تحكم العلامة التجارية

**المرجع:** القسم 18-هـ (كامل، الشاشات هـ.1 إلى هـ.9).

**المهام (Flutter):** تحديث `brand_dashboard_screen.dart` وشاشات دليل المنتجات/خريطة التوزيع/شريحة المستهلكين/دعم المحلات المبنية بالفازة 8 وما بعدها، بنفس هوية الفازة 22 تمامًا (`merchantBrandTheme`):
- القسم 18-هـ.2 (دليل المنتجات: شبكة صور + باركود).
- القسم 18-هـ.3 (خريطة التوزيع: Heatmap ذهبي أحادي اللون حصرًا، لا تدرّج ألوان متعدد تقليدي).
- القسم 18-هـ.4 (شريحة المستهلكين: `--gold` و`--mint` معًا للتمييز الديموغرافي — هذا الاستثناء الوحيد الموثّق لاستخدام `--mint` خارج شاشة المحفظة).
- القسم 18-هـ.7 (دعم المحلات: قائمة مرتّبة برقم دائري ذهبي).
- باقي الشاشات (هـ.1، هـ.5، هـ.6، هـ.8، هـ.9) مطابقة حرفيًا لنظيراتها بلوحة التاجر (د.1، د.5، د.6، د.8، د.11).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ نفس فحوصات الفازة 22 مطبّقة على شاشات لوحة العلامة التجارية.
+ تحقق خاص: `kMint` يظهر فقط داخل شاشة شريحة المستهلكين (هـ.4) ولا يظهر بأي شاشة أخرى من لوحة العلامة.

**معيار القبول:** "لوحة العلامة التجارية جاهزة بصريًا عندما تطابق هوية لوحة التاجر البصرية بالكامل (`--indigo`)، مع الاستثناء الوحيد الموثّق لاستخدام `--mint` في شاشة شريحة المستهلكين تحديدًا، وHeatmap خريطة التوزيع بتدرّج ذهبي أحادي فقط."

---

## الفازة 24 — تطبيق نظام التصميم على لوحة إدارة كوبونا (Admin)

**المرجع:** القسم 18-و (كامل، الشاشات و.1 إلى و.9).

**المهام (Flutter):** بناء/تحديث شاشات لوحة الأدمن (طلبات العضوية، العمليات الحية، إدارة المحركات، إعدادات الفترة التجريبية، إدارة الاشتراكات، التسويات المالية، سجلات الاحتيال، مراجعة إعلانات الأفراد، الدعم الفني — المبنية جزئيًا بالفازات 3، 7، 10، 13، 15، 16) لتستخدم `adminTheme` (`--ink` كامل، Dark Mode):
- إضافة لون تحذيري استثنائي جديد لهذه اللوحة فقط: `kFraudRed = #C0524A` إلى `design_tokens.dart` (امتداد على الفازة 19، محصور صراحة بهذه اللوحة).
- تطبيق `KupunaStatusPill` على عمود الحالة في القسم 18-و.5 (إدارة الاشتراكات) بالألوان الأربعة المحددة (تجربة ذهبي/نشط تيل/سماح برتقالي مطفأ/معطّل رمادي — يتطلب توسيع `enum StatusPillKind` بحالتين إضافيتين: سماح، معطّل).

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ فحص نصي: `kFraudRed` (أو ما يعادله من قيمة Hex `#C0524A`) لا يظهر خارج شاشات لوحة الأدمن.
+ فحص عدم اختلاط الألوان: صفر نتائج لـ `kTeal|kMint|kIndigo|kViolet` داخل شاشات لوحة الأدمن (تُبنى بالكامل على `kInk`/`kGold`/`kFraudRed` فقط).

**معيار القبول:** "لوحة الأدمن جاهزة بصريًا عندما تكون خلفيتها `--ink` بالكامل (Dark Mode)، مع الذهبي للأرقام المهمة فقط والأحمر المطفأ حصريًا لتنبيهات الاحتيال الحرجة، دون أي تسرّب من ألوان الأوضاع الأخرى."

---

## الفازة 25 — تدقيق شامل نهائي لنظام التصميم (Global Design QA)

**المرجع:** القسم 18-ز (كامل).

**المهام:**
- **لا بناء جديد.** هذه فازة تدقيق وتغطية شاملة (بنفس فلسفة الفازة 16 للمنطق الوظيفي)، تُطبَّق على **كل شاشة بالتطبيق دون استثناء**، بما فيها أي شاشة لم تُذكر تفصيلًا صريحًا بالقسم 18 (تتبع القاعدة العامة 18-ز تلقائيًا).
- فحص شامل (Regex/grep) عبر **كل** ملفات `lib/screens/*.dart` و`lib/widgets/*.dart`:
  1. صفر نتائج لأي `Colors.` من مجموعة الألوان المدمجة في Flutter (deepPurple، blue، red، إلخ) — كل لون يجب أن يأتي من `design_tokens.dart`.
  2. صفر نتائج لأي قيمة `Color(0xFF...)` مكتوبة مباشرة خارج `lib/theme/design_tokens.dart` نفسه.
  3. لا خلط بين `kTeal`/`kMint` (عميل) و`kIndigo`/`kIndigoLight` (تاجر/علامة) في نفس ملف الشاشة الواحد — عدا الاستثناءات الموثّقة صراحة أعلاه (شاشة التسجيل د.1/هـ.1 بخلفية `--sand`، شاشة شريحة المستهلكين هـ.4 بـ`--mint`).
  4. `kViolet` لا يظهر إلا في شاشة/مكوّن وضع الكاشير حصرًا.
- تدقيق i18n مرتبط بالتصميم: التأكد أن أي نص جديد أُضيف أثناء الفازات 19-24 (تسميات شارات، عناوين تبويبات) مُضاف كمفتاح `.tr()` في **كل** ملفات `assets/lang/*.json` (16 لغة) — لا نص عربي/إنجليزي مباشر جديد.
- مراجعة يدوية نهائية لكل بند بجدول القسم 18-ز كقائمة تحقق (Checklist) داخل تقرير التسليم.
- تقرير تسليم نهائي `KUPUNA_DESIGN_SYSTEM_DELIVERY_REPORT.md` يوثّق: تاريخ إنجاز كل فازة (19-25)، نتيجة كل أمر تحقق، ولقطات شاشة (أو وصف نصي دقيق) قبل/بعد لكل شاشة رئيسية.

**أمر التحقق:**
```powershell
flutter analyze
flutter test
```
+ سكربت فحص واحد شامل (`scripts/design_audit.ps1` أو ما يعادله) يمر على كل نقاط الفحص الأربع أعلاه دفعة واحدة عبر شجرة `lib/` كاملة، ويطبع تقريرًا بعدد المخالفات (يجب أن يكون صفرًا للقبول).

**معيار القبول:** "نظام التصميم جاهز للتسليم النهائي عندما يُظهر سكربت التدقيق الشامل صفر مخالفات عبر كل شاشات `lib/`، ولا يتبقى أي لون/قياس مكتوب مباشرة خارج `design_tokens.dart`، ولا أي اختلاط بين ألوان الأوضاع المختلفة عدا الاستثناءات الموثّقة صراحة بالقسم 18-ز."

---

## ملاحظة ختامية للـ Agent المُنفِّذ

- هذه الوثيقة تصف منصة كاملة (13 محركًا، أدوار متعددة، اشتراكات، احتيال، تسويات) **+ نظام تصميم بصري كامل** (تصميم موحّد لكل شاشة عبر 4 أوضاع لونية) — **حجم العمل يوازي مشروعًا متعدد الأشهر**، ليس Sprint واحدًا. التزم بالترتيب ولا تحاول دمج فازتين لتوفير الوقت؛ الوثيقة نفسها تنص: *"لا يُبنى أي محرك أو شاشة قبل اعتماد User الموحّد (Sprint 0)"*، وبالمثل *"لا تُطبَّق فازات التصميم (21-24) على أي شاشة قبل اكتمال Design Tokens ومكتبة المكوّنات (19-20)"*.
- إذا توقفت الجلسة في منتصف فازة، السجل المرجعي دائمًا هو: (1) هذا الملف لمعرفة أين توقفت، (2) `PHASE_<n>_<slug>_REPORT.md` الخاص بآخر فازة مكتملة، (3) `/memories/repo/kupuna-ops.md` للقواعد التشغيلية العامة لهذا المستودع.
- عند أي تعارض بين جزء من هذه الخطة وتفصيل دقيق في `docs/KUPUNA_MASTER_SPEC.md` الأصلي (بما فيه القسم 18 الجديد الخاص بالتصميم)، **الأصل يُرجَّح دائمًا**، وهذا الملف يُحدَّث ليطابقه.
