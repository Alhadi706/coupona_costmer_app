# Pro-Rata Multi-Sponsor Coalition Engine v3 - تقرير التنفيذ

## 🚀 نظرة عامة

تم تنفيذ نظام الائتلاف متعدد الرعاة بالنسبة والتناسب (Pro-Rata) بالكامل، بما يشمل:
- نظام تقييم نقاط ديناميكي مع مضاعف تسويقي
- تسعير مرن للتجار والإهداء المتبادل المستهدف
- تصنيف مرئي للائتلافات في واجهة العميل
- دفتر تسوية متعدد الرعاة بالنسبة والتناسب
- حوار شكر مشترك متعدد الرعاes

---

## 📊 المكونات المنفذة

### 1. الباك إند (Backend)

#### قاعدة البيانات (Schema)
**ملف:** `backend/src/schema-coalition.js`

جداول جديدة:
- `coalition_gift_catalog`: كتالوج الهدايا المشتركة
  - يحتوي على: العنوان، الوصف، النقاط المطلوبة، القيمة النقدية، نوع الحملة، نسبة الخصم، الحد الكمي، تاريخ الانتهاء
  
- `coalition_redemption_splits`: سجل الاستبدالات مع تقسيم Pro-Rata
  - يتتبع: الهدية المستبدلة، العميل، التاجر المُنفذ، إجمالي النقاط المستخدمة
  
- `coalition_redemption_contributions`: مساهمات التجار الفردية في كل استبدال
  - يحتوي على: معرف الاستبدال، التاجر المساهم، النقاط المساهم بها، نسبة المساهمة
  
- `customer_merchant_point_balances`: رصيد النقاط لكل عميل عند كل تاجر
  - يسمح بحساب Pro-Rata بدقة عند الاستبدال
  
- `coalition_gift_targeting`: استهداف محدد للحملات
  - يدعم: عملاء جدد، عملاء VIP، تكرار الشراء، أيام منذ آخر زيارة

#### خدمة تقييم النقاط (Point Valuation Service)
**ملف:** `backend/src/point-valuation-service.js`

وظائف:
- `calculatePointsEarned(purchaseAmount, merchantEarningRate)`: حساب النقاط المكتسبة
  - **المعادلة:** 1 نقطة = 0.10 وحدة عملة محلية
  - **مثال:** شراء 100 ليرة ليبية بنسبة أرباح 10% = 100 نقطة (قيمة 10 ليرة)

- `pointsToMonetary(points)`: تحويل النقاط إلى قيمة نقدية

- `monetaryToPoints(monetaryValue)`: تحويل القيمة النقدية إلى نقاط

- `applyDiscountToPoints(basePoints, discountPercentage)`: تطبيق خصم على حد النقاط

- `qualifiesForCampaign(customer, targeting)`: فحص أهلية العميل للحملة

- `calculateProRataSplit(pointBalances, totalPointsNeeded)`: حساب تقسيم Pro-Rata
  - يوزع النقاط المستخدمة حسب مساهمة كل تاجر
  - **مثال:** عميل لديه 50 نقطة من متجر A + 30 من B + 20 من C = 100 نقطة
  - النتيجة: A يساهم 50% + B يساهم 30% + C يساهم 20%

- `formatCoBrandedMessage(sponsors)`: تنسيق رسالة الشكر متعددة الرعاة

#### نقاط النهاية (API Endpoints)
**ملف:** `backend/src/routes/coalition.js`

نقاط نهاية جديدة:

1. **إنشاء هدية مشتركة**
   ```
   POST /api/merchant/coalitions/:id/gifts
   ```
   - إنشاء هدية في كتالوج الائتلاف
   - يدعم أنواع الحملات: قياسي، استقطاب عملاء جدد، VIP
   - يدعم الخصومات والاستهداف المحدد

2. **الحصول على كتالوج الهدايا**
   ```
   GET /api/merchant/coalitions/:id/gifts
   ```
   - عرض جميع الهدايا النشطة في الائتلاف

3. **الحصول على أرصدة العميل**
   ```
   GET /api/customer/coalitions/:id/balances
   ```
   - عرض رصيد النقاط لدى كل تاجر في الائتلاف
   - إجمالي النقاط المتاحة

4. **استبدال هدية مع Pro-Rata**
   ```
   POST /api/customer/coalitions/redeem-gift
   ```
   - استبدال هدية باستخدام نقاط من تجار متعددين
   - حساب تلقائي للتقسيم Pro-Rata
   - تسجيل في دفتر المقاصة (clearinghouse)
   - إرسال إشعار مع رسالة شكر متعددة الرعاة

5. **تقرير تأثير الائتلاف**
   ```
   GET /api/merchant/coalitions/:id/gift-impact
   ```
   - إحصائيات مساهمات التاجر
   - إحصائيات الهدايا المنفذة
   - قائمة العملاء المتقاطعين مؤخراً

#### تكامل مع منح النقاط
**ملف:** `backend/src/routes/wallet-actions.js`

تحديث:
- عند منح نقاط للعميل، يتم تحديث `customer_merchant_point_balances` تلقائياً
- يتم تسجيل النقاط لكل ائتلاف ينتمي له التاجر

---

### 2. الواجهات (Frontend)

#### واجهة كتالوج الهدايا المشتركة
**ملف:** `lib/screens/coalitions/coalition_gift_catalog_screen.dart`

مميزات:
- عرض جميع الهدايا المشتركة في الائتلاف
- إنشاء هدية جديدة مع حوار تفصيلي:
  - العنوان والوصف
  - النقاط المطلوبة والقيمة النقدية
  - نوع الحملة (قياسي / عملاء جدد / VIP)
  - نسبة خصم اختيارية
  - حد كمي وتاريخ انتهاء
  - استهداف محدد (عملاء جدد / VIP / تكرار / أيام منذ آخر زيارة)
- عرض بطاقات الهدايا مع:
  - شارات للحملات المستهدفة (🎯 عملاء جدد / VIP)
  - عداد الكمية المستبدلة
  - تاريخ الانتهاء
  - نسبة الخصم

#### واجهة تقرير تأثير الائتلاف
**ملف:** `lib/screens/coalitions/coalition_impact_report_screen.dart`

أقسام:
1. **مساهماتك (As Contributor):**
   - عدد العملاء الفريدين
   - إجمالي النقاط المساهم بها
   - متوسط نسبة المساهمة

2. **كمُنفذ للهدايا (As Fulfiller):**
   - عدد العملاء الذين استلموا هدايا
   - إجمالي النقاط المستلمة

3. **عملاء متقاطعون حديثون:**
   - قائمة العملاء الذين استبدلوا هدايا
   - تفاصيل مساهمة التاجر لكل عميل
   - تاريخ الاستبدال

#### widget شارات الائتلافات
**ملف:** `lib/widgets/coalition_badge.dart`

أنواع الشارات:
- 🟢 **عضو شبكة عامة (Public Network):** النقاط تُستخدم لقسائم على مستوى المنصة
- 🔵 **عضو ائتلاف خاص (Private Coalition):** شارات ائتلاف، قائمة شركاء، نقاط مجمّعة
- ⚪ **تاجر مستقل (Standalone):** النقاط مقفلة للاستبدال داخل المتجر فقط

الاستخدام:
```dart
CoalitionBadge(
  type: CoalitionBadgeType.privateCoalition,
  showDescription: true,
)
```

#### حوار استبدال الهدية للعميل
**ملف:** `lib/dialogs/customer_redeem_coalition_gift_dialog.dart`

مميزات:
- عرض رصيد النقاط الإجمالي للعميل
- قائمة الهدايا المتاحة مع:
  - إمكانية اختيار هدية واحدة
  - عرض النقاط المطلوبة
  - تعطيل الهدايا غير المتاحة (نقاط غير كافية)
- عند الاستبدال الناجح:
  - حوار تهنئة مع أيقونة احتفال 🎉
  - عرض الهدية المستبدلة
  - قائمة الرعاة المساهمين:
    - اسم التاجر
    - عدد النقاط المساهم بها
    - نسبة المساهمة (%)
  - رسالة شكر متعددة الرعاة (Co-Branded Message)

---

### 3. خدمات الـ API (CompanyServerService)
**ملف:** `lib/services/company_server_service.dart`

دوال جديدة:
- `getCoalitionGiftCatalog(coalitionId)`: جلب كتالوج الهدايا
- `createCoalitionGift(...)`: إنشاء هدية مشتركة
- `getCustomerCoalitionBalances(coalitionId)`: جلب أرصدة العميل
- `redeemCoalitionGift(giftId, fulfillerMerchantId)`: استبدال هدية
- `getCoalitionImpactReport(coalitionId)`: جلب تقرير التأثير

---

## 🌐 التعريب (Localization)

تمت إضافة ~80 مفتاح جديد في `assets/lang/ar.json` و `assets/lang/en.json`:

مجموعات المفاتيح:
- `coalition_gift_*`: كتالوج الهدايا والإنشاء
- `customer_redeem_*`: استبدال الهدايا وحوار النجاح
- `coalition_badge_*`: شارات الائتلافات
- `coalition_impact_*`: تقرير التأثير

---

## 🔄 آلية Pro-Rata التفصيلية

### مثال عملي:

**السيناريو:**
- عميل لديه:
  - 50 نقطة من "مطعم النخيل"
  - 30 نقطة من "مقهى الشموخ"
  - 20 نقطة من "محل الإطارات"
- هدية متاحة: وجبة مجانية تتطلب 100 نقطة
- التاجر المُنفذ: "مطعم النخيل"

**الحساب:**
1. النظام يحسب تلقائياً:
   - مطعم النخيل يساهم بـ 50 نقطة (50%)
   - مقهى الشموخ يساهم بـ 30 نقطة (30%)
   - محل الإطارات يساهم بـ 20 نقطة (20%)

2. التسجيل في دفتر المقاصة:
   - يتم إضافة 30 نقطة كدين على "مقهى الشموخ" لصالح "مطعم النخيل"
   - يتم إضافة 20 نقطة كدين على "محل الإطارات" لصالح "مطعم النخيل"

3. رسالة الشكر للعميل:
```
هذه الهدية برعاية مشتركة من:
• مطعم النخيل (50%)
• مقهى الشموخ (30%)
• محل الإطارات (20%)
تقديراً لولائك المشترك لشركائنا في الائتلاف.
```

---

## 📈 المزايا التسويقية

### 1. تقييم النقاط الديناميكي
- **1 نقطة = 0.10 وحدة عملة محلية**
- يخلق عدادات سريعة الحركة تزيد من الإدراك النفسي للمكافأة
- التجار يضبطون نسبة الأرباح حسب هوامشهم:
  - مقهى هامش عالي: 100 نقطة لكل 100 ليرة (10%)
  - بقالة هامش منخفض: 20 نقطة لكل 100 ليرة (2%)

### 2. التسعير المرن
- **حملة استقطاب عملاء جدد:** خصومات للعملاء الذين لم يشتروا من المتجر
- **حملة VIP:** خصومات لعملاء ذوي تكرار شراء عالي
- **إشعارات ترحيب تلقائية:** عند تأهل العميل من شركاء الائتلاف

### 3. التصنيف المرئي
- شارات واضحة توضح نوع العضوية
- سهولة فهم نطاق استخدام النقاط
- تشجيع الانضمام للائتلافات

### 4. الشفافية الكاملة
- العميل يرى بوضوح من ساهم في هديته
- التجار يتابعون تأثيرهم المتبادل
- يبني الثقة والولاء

---

## 🔧 الاختبار والنشر

### النتائج:
✅ **Backend:**
- جميع ملفات JS اجتازت فحص Syntax
- الجداول الجديدة جاهزة للإنشاء عند بدء السيرفر
- نقاط النهاية مسجلة بنجاح

✅ **Frontend:**
- جميع ملفات Dart اجتازت فحص `flutter analyze`
- لا توجد أخطاء
- التعريب متكامل (عربي + إنجليزي)

✅ **Build & Deploy:**
- بناء الويب نجح بدون أخطاء (72.7 ثانية)
- نُشر في `web_deploy/`
- حاويات Docker أعيد تشغيلها بنجاح

---

## 📝 خطوات الاستخدام للتجار

### إنشاء هدية مشتركة:
1. افتح "الائتلافات" → اختر ائتلاف → اضغط "كتالوج الهدايا المشتركة"
2. اضغط "إنشاء هدية مشتركة"
3. املأ البيانات:
   - عنوان الهدية ووصفها
   - عدد النقاط المطلوبة
   - اختر نوع الحملة (قياسي / عملاء جدد / VIP)
   - أضف خصم اختياري
4. اضغط "إنشاء الهدية"

### متابعة التأثير:
1. افتح "الائتلافات" → اختر ائتلاف
2. انتقل إلى "تقرير تأثير الائتلاف"
3. راجع:
   - كم عميل ساهمت في هداياهم
   - كم نقطة ساهمت
   - من الشركاء الذين استفادوا من نقاطك
   - من العملاء المشتركون

---

## 📝 خطوات الاستخدام للعملاء

### استبدال هدية من الائتلاف:
1. افتح قائمة الائتلافات
2. اختر الائتلاف واضغط "استبدال هدية"
3. شاهد رصيدك الإجمالي من النقاط
4. اختر الهدية المطلوبة
5. اضغط "تأكيد الاستبدال"
6. سترى:
   - تهنئة بنجاح الاستبدال 🎉
   - قائمة الرعاة الذين ساهموا في هديتك
   - رسالة شكر مشتركة

---

## 🎯 الملفات المعدلة/المنشأة

### Backend (5 ملفات):
- `backend/src/schema-coalition.js` ✏️ معدّل
- `backend/src/point-valuation-service.js` ✅ جديد
- `backend/src/routes/coalition.js` ✏️ معدّل
- `backend/src/routes/wallet-actions.js` ✏️ معدّل

### Frontend (5 ملفات):
- `lib/screens/coalitions/coalition_gift_catalog_screen.dart` ✅ جديد
- `lib/screens/coalitions/coalition_impact_report_screen.dart` ✅ جديد
- `lib/widgets/coalition_badge.dart` ✅ جديد
- `lib/dialogs/customer_redeem_coalition_gift_dialog.dart` ✅ جديد
- `lib/services/company_server_service.dart` ✏️ معدّل

### Localization (2 ملفات):
- `assets/lang/ar.json` ✏️ معدّل (+80 مفتاح)
- `assets/lang/en.json` ✏️ معدّل (+80 مفتاح)

---

## ✅ الخلاصة

تم تنفيذ **Pro-Rata Multi-Sponsor Coalition Engine v3** بالكامل وفقاً للمواصفات المطلوبة:

1. ✅ نظام تقييم نقاط ديناميكي (1 نقطة = 0.10 عملة)
2. ✅ تسعير مرن مع حملات مستهدفة (عملاء جدد / VIP)
3. ✅ شارات مرئية للتصنيف (🟢🔵⚪)
4. ✅ دفتر تسوية Pro-Rata متعدد الرعاة
5. ✅ حوار شكر مشترك متعدد الرعاة (Co-Branded Gratitude)

**النظام جاهز للاستخدام الفوري!**

---

التاريخ: 2026-08-25
المطور: GitHub Copilot (Claude Sonnet 4.5)
