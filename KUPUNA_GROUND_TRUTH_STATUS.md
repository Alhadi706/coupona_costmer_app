# حالة الحقيقة المؤكدة (Ground Truth) — 2026-08-07

> هذا الملف يوثّق ما تم التحقق منه فعليًا بالقراءة المباشرة للكود (وليس فقط بتصريحات تقارير الفازات)، ليكون مرجعًا موثوقًا لمتابعة العمل. حدّثه بعد أي تحقق مستقل جديد فقط، لا بتصريحات غير مثبتة.

## 1) الباك-إند (backend/server.js + migrations 001-016)
- **مؤكد فعليًا (عبر PowerShell `Select-String`، وليس أداة grep المعطوبة مع هذا الملف تحديدًا):**
  - 103 مسار Express حقيقي (`app.get/post/put/patch/delete`).
  - `community_groups` مستخدم بمنطق حقيقي: إنشاء تلقائي (`INSERT ... ON CONFLICT (role_type, role_profile_id) DO NOTHING`) + استعلامات ربط.
  - `offer_targeting_rules`, `FCM_SERVER_KEY`, `cashier`, `branch_manager_permissions`, `escrow_accounts`, `reward_claims` كلها موجودة بتكرارات حقيقية (4-28 مطابقة لكل واحد) تدل على منطق فعلي لا مجرد ذكر عابر.
  - migrations 001 إلى 016 موجودة بترقيم تسلسلي سليم بدون فجوات.
  - `backend/.env` غير موجود حاليًا في مساحة العمل، ولم يُسجَّل إطلاقًا في تاريخ Git (`git log --all -- backend/.env` فارغ) — لا تسريب سر فعلي حدث.
- **غير مُتحقَّق منه بعد بعمق (يحتاج مراجعة تفصيلية لاحقة قبل القبول النهائي):**
  - صحة كل معادلة رياضية (كسور، تبادل، نسب) مقارنة بالقسم 4 من `docs/KUPUNA_MASTER_SPEC.md` سطرًا بسطر.
  - تكامل FCM الفعلي (هل يُرسل إشعارات حقيقية أم فقط يحتفظ بمفتاح `FCM_SERVER_KEY` دون استدعاء فعلي؟) — يحتاج تتبّع لمكان استخدام هذا المتغير وتأكيد استدعاء API فعلي لـ FCM.

## 2) واجهات Flutter — الفجوة الحقيقية المؤكدة (بقراءة مباشرة، موثوقة 100%)
- ✅ **دور العميل مكتمل وشغّال**: كل الشاشات في `lib/screens/` المتعلقة بالعميل (تسجيل دخول، عروض، محفظة، نقاط، مكافآت، مجتمع، بلاغات، مسح فاتورة، أدواري/تبديل دور) مربوطة فعليًا بـ`CompanyServerService` ومسارات سيرفر حقيقية.
- ❌ **لوحة التاجر (Merchant Dashboard)**: لا وجود لها — `home_screen.dart` يعرض فقط `_buildRolePlaceholder()` (عنوان + نص عام) عند اختيار دور "تاجر". لا شاشة فروع، لا مراجعة فواتير، لا موظفين، لا عروض تاجر منفصلة، لا تسويات.
- ❌ **لوحة العلامة التجارية (Brand Dashboard)**: نفس الحالة — Placeholder فقط.
- ❌ **وضع الكاشير**: Placeholder فقط، لا ماسح QR فعلي لمنح نقاط أو تسليم مكافأة رغم وجود منطق الكاشير بالباك-إند.
- ❌ **`lib/screens/points_conversion_screen.dart`**: ملف فارغ تمامًا (0 بايت محتوى فعلي).
- ❌ **`lib/screens/reward_qr_code_screen.dart`**: يحتوي على مسافات بيضاء فقط، لا كود.
- ❌ لا وجود لإدارة الفروع/الموظفين، دليل المنتجات/خريطة التوزيع، إعلانات الأفراد (Peer Ads)، Loyalty Health Score، أو لوحة إدارة كوبونا (Admin Panel) في أي مكان داخل `lib/`.

## 3) الخلاصة العملية
- الباك-إند متقدم بشكل حقيقي عبر الفازات 4-17 (بفرض دقة المنطق الداخلي التي لم تُراجَع بعد بالكامل).
- **تطبيق Flutter لا يزال عمليًا تطبيق "عميل فقط"** — أي دور آخر غير قابل للاستخدام الفعلي من طرف أي مستخدم حقيقي مهما كان الباك-إند جاهزًا خلفه.
- **الأولوية القصوى التالية:** بناء لوحة التاجر، لوحة العلامة التجارية، وضع الكاشير الفعلي، وإكمال شاشتَي التبادل واستلام المكافأة — هذا العمل لم يبدأ فعليًا رغم أن الخطة الأصلية خصّصت لكل فازة backend مهمة Flutter صغيرة فقط (تحديثات طفيفة على شاشات موجودة)، وهذا على الأرجح **قصور في تفصيل الخطة نفسها**، وليس فقط في التنفيذ — يُنصح بإضافة فازة/فازات مخصصة صراحة لبناء هذه اللوحات الثلاث بالكامل، لأن الخطة الحالية لم تخصص لها مهام Flutter كافية أصلًا.

## 4) قاعدة إلزامية للمتابعة (أي إيجنت، حالي أو جديد)
لا تقبل أي عبارة "PASS" أو "Implemented" بدون دليل مرفق (مخرجات `Select-String`/`grep` فعلية، أو diff، أو استجابة JSON حقيقية من endpoint). التحقق مؤخرًا أظهر أن أداة grep الافتراضية قد تُعطي نتائج فارغة خاطئة مع `backend/server.js` تحديدًا — استخدم `Select-String` (PowerShell) كبديل موثوق عند التحقق من هذا الملف تحديدًا.
